name: fastapi-crud
service:
  fastapi:
    public: true
    image: fastapi-okteto:latest
    build:
      context: .
      dockerfile: Dockerfile
    replicas: 1
    ports:
      - 8080
    resources:
      cpu: 100m
      memory: 128Mi
